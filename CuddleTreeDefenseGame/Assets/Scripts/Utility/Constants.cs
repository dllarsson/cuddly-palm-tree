using System;

public static class Constants
{
    //readonly instead of const because typeof cannot be constant
    //Disabled until implementation
    //public static readonly Type StateBaseClass = typeof(State);
}
